const FULL_PET_WORKFLOW_MAX_DURATION_MS = 90 * 60 * 1000
const FULL_PET_COMMAND_SHUTDOWN_GRACE_MS = 5 * 60 * 1000
const FULL_PET_COMMAND_TIMEOUT_MS = FULL_PET_WORKFLOW_MAX_DURATION_MS + FULL_PET_COMMAND_SHUTDOWN_GRACE_MS
const GENERATION_LEASE_STALE_AFTER_MS = FULL_PET_COMMAND_SHUTDOWN_GRACE_MS
const GENERATION_LEASE_HEARTBEAT_INTERVAL_MS = 30 * 1000
const GENERATION_COMMAND_TERMINATED_REASON = 'generation-command-terminated'

module.exports = {
  FULL_PET_COMMAND_SHUTDOWN_GRACE_MS,
  FULL_PET_COMMAND_TIMEOUT_MS,
  FULL_PET_WORKFLOW_MAX_DURATION_MS,
  GENERATION_COMMAND_TERMINATED_REASON,
  GENERATION_LEASE_HEARTBEAT_INTERVAL_MS,
  GENERATION_LEASE_STALE_AFTER_MS
}
