{
  "title": "IM Gateway",
  "description": "Control non-secret IM trigger policy for the bundled OpenPet IM Gateway.",
  "type": "object",
  "properties": {
    "telegramEnabled": {
      "type": "boolean",
      "title": "Enable Telegram",
      "description": "Enable the Telegram adapter when the IM Gateway service starts.",
      "default": false
    },
    "qqEnabled": {
      "type": "boolean",
      "title": "Enable QQ official robot",
      "description": "Enable the official QQ Robot adapter. Credentials are stored by the host and are never part of this config.",
      "default": false
    },
    "telegramMode": {
      "type": "string",
      "title": "Telegram mode",
      "description": "Phase 1 supports long polling only.",
      "enum": ["polling"],
      "default": "polling"
    },
    "wecomEnabled": {
      "type": "boolean",
      "title": "Enable WeCom",
      "description": "Enable the WeCom self-built application callback adapter. Disabled by default.",
      "default": false
    },
    "wecomMode": {
      "type": "string",
      "title": "WeCom mode",
      "description": "WeCom uses a verified callback endpoint and HTTPS message delivery.",
      "enum": ["callback"],
      "default": "callback"
    },
    "wecomCorpId": {
      "type": "string",
      "title": "WeCom Corp ID",
      "description": "Tenant identifier used to request a session token.",
      "default": ""
    },
    "wecomAgentId": {
      "type": "number",
      "title": "WeCom Agent ID",
      "description": "Self-built application agent id.",
      "default": 0
    },
    "wecomCallbackPath": {
      "type": "string",
      "title": "WeCom callback path",
      "description": "Loopback service callback path to expose through the configured reverse proxy.",
      "default": "/wecom/callback"
    },
    "privateTextMode": {
      "type": "string",
      "title": "Private text mode",
      "description": "Choose how ordinary private non-command text routes inside Telegram.",
      "enum": ["command-only", "pet-say", "ai-chat"],
      "default": "command-only"
    },
    "groupChatPolicy": {
      "type": "string",
      "title": "Group chat policy",
      "description": "Choose how group chats can trigger OpenPet.",
      "enum": ["mention-or-command", "command-only"],
      "default": "mention-or-command"
    },
    "groupAiRepliesEnabled": {
      "type": "boolean",
      "title": "Enable group AI replies",
      "description": "Allow host-owned AI replies for direct bot mentions in allowed groups.",
      "default": false
    },
    "allowedUsers": {
      "type": "string",
      "title": "Allowed users",
      "description": "Comma-separated Telegram user ids. Group chats always require an allowed user.",
      "default": ""
    },
    "allowedChats": {
      "type": "string",
      "title": "Allowed chats",
      "description": "Comma-separated Telegram chat ids for group or channel contexts.",
      "default": ""
    },
    "allowAllPrivateChats": {
      "type": "boolean",
      "title": "Allow all private chats",
      "description": "Allow private chat triggers from any user.",
      "default": false
    },
    "allowAllGroupChats": {
      "type": "boolean",
      "title": "Allow all group chats",
      "description": "Allow group chat ids without listing each chat. The sender must still be an allowed user.",
      "default": false
    },
    "commandAliases": {
      "type": "string",
      "title": "Command aliases",
      "description": "Comma-separated command aliases.",
      "default": "/openpet,/op"
    },
    "petSayTtlMs": {
      "type": "number",
      "title": "Speech TTL",
      "description": "Milliseconds to show pet.say text from IM triggers.",
      "default": 6000
    },
    "receiptMode": {
      "type": "string",
      "title": "Receipt mode",
      "description": "Choose when command receipts are sent back to IM.",
      "enum": ["commands-only", "none"],
      "default": "commands-only"
    }
  }
}
